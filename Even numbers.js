const areOdd = (n, m) => {
    //condicion si son divisibles para dos tanti n y m 
    if (n%2===0 && m%2===0){
        return true;
    } else {return false};
    // if retorna true
    //elsa retorna false


};
