/*Dado un array de números enteros de longitud 3, devuelve un 
nuevo array con los elementos en orden inverso, por lo que [1, 2, 3] 
 convierte en [3, 2, 1].*/
 function reverse (arr){
    return arr.reverse();
 }